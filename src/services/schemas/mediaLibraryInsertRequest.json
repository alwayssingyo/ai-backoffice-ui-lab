{
	"type": "object",
	"properties": {
		"files": {
			"type": "array",
			"description": "업로드된 파일 메타 목록",
			"items": {
				"type": "object",
				"properties": {
					"url": {"type": "string", "description": "파일 URL", "minLength": 1},
					"path": {"type": "string", "description": "스토리지 경로", "minLength": 1},
					"mimeType": {"type": "string", "description": "MIME 타입", "minLength": 1},
					"name": {"type": "string", "description": "파일명", "minLength": 1},
					"size": {"type": "integer", "format": "int64", "description": "파일 크기(byte)", "minimum": 1},
					"ext": {"type": "string", "description": "확장자"},
					"provider": {"type": "string", "description": "스토리지 제공자", "enum": ["AWS", "GCP", "NCP", "LOCAL"]}
				},
				"required": ["ext", "mimeType", "name", "path", "provider", "size", "url"],
				"x-readme-ref-name": "UpsertMediaFileRequest"
			}
		}
	},
	"required": ["files"],
	"x-readme-ref-name": "MediaLibraryInsertRequest"
}
