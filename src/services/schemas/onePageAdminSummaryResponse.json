{
	"type": "object",
	"properties": {
		"page": {"type": "integer", "format": "int32"},
		"totalCount": {"type": "integer", "format": "int32"},
		"list": {
			"type": "array",
			"items": {
				"type": "object",
				"properties": {
					"id": {"type": "integer", "format": "int64", "description": "어드민 아이디"},
					"permissionGroupName": {"type": "string", "description": "권한"},
					"provider": {"type": "string", "description": "소셜 유형", "enum": ["GOOGLE", "APPLE"]},
					"loginId": {"type": "string", "description": "로그인 아이디"},
					"name": {"type": "string", "description": "이름"},
					"removed": {"type": "boolean", "description": "삭제 여부"},
					"lockedAt": {"type": "string", "format": "date-time", "description": "잠금 일시"},
					"lastLoginAt": {"type": "string", "format": "date-time", "description": "마지막 로그인 일시"},
					"createdAt": {"type": "string", "format": "date-time", "description": "생성 일시"}
				},
				"required": ["id", "loginId", "name", "permissionGroupName", "removed"],
				"x-readme-ref-name": "AdminSummaryResponse"
			}
		}
	},
	"required": ["list", "page", "totalCount"],
	"x-readme-ref-name": "OnePageAdminSummaryResponse"
}
