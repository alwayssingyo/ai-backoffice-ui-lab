{
	"type": "object",
	"properties": {
		"permissionGroupRequest": {
			"description": "권한 그룹 정보",
			"type": "object",
			"properties": {
				"id": {"type": "integer", "format": "int64", "description": "권한 그룹 아이디 - null이면 등록, 아니면 수정,"},
				"name": {"type": "string", "description": "권한 그룹명", "minLength": 1},
				"description": {"type": "string", "description": "설명"}
			},
			"required": ["name"],
			"x-readme-ref-name": "PermissionGroupSaveRequest"
		},
		"apiEndpoints": {
			"type": "array",
			"description": "할당 API 엔드포인트 권한 목록",
			"items": {
				"type": "object",
				"properties": {
					"tagName": {"type": "string", "description": "Tag name", "minLength": 1},
					"httpMethods": {
						"type": "array",
						"description": "사용 가능한 HTTP 메서드 목록",
						"items": {"type": "string", "enum": ["GET", "POST", "PUT", "DELETE"]},
						"uniqueItems": true
					}
				},
				"required": ["httpMethods", "tagName"],
				"x-readme-ref-name": "ApiEndpointSaveRequest"
			}
		}
	},
	"required": ["permissionGroupRequest"],
	"x-readme-ref-name": "PermissionGroupApiEndpointSaveRequest"
}
